defmodule Bread.Rooms.QueueItem do

  use Ecto.Schema
  # import Ecto.Changeset

  # alias Bread.Accounts.User
  # alias Bread.Rooms.Link

  # schema "queue_item" do


  #   timestamps()
  # end

  # def changeset model, params \\ %{} do
  #   model
  #   |> cast(params, [:user_id, :link_id, :room_id, :position])
  #   |> validate_format(:name, ~r/^[a-zA-Z0-9_]+$/, message: "Room name should only contain numbers, letters and underscores")
  #   |> validate_required([:name, :user_id])
  #   |> unique_constraint(:name)
  #   |> validate_length(:name, max: 64)
  #   |> validate_length(:title, max: 100)
  #   |> foreign_key_constraint(:user_id)
  # end
  
end